<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <item name="android:actionBarItemBackground">@drawable/selectable_background_actionbar</item>
        <item name="android:homeAsUpIndicator">@drawable/custom_icon_left</item>
        <item name="android:actionBarStyle">@style/MyActionBar</item>
        <item name="android:actionMenuTextColor">@color/red</item>
     	<item name="android:actionBarTabTextStyle">@style/MyActionBarTabText</item>
		<item name="android:actionBarSize">@dimen/height_header</item>
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="MyActionBar" parent="@android:style/Widget.Holo.Light.ActionBar">
        <item name="android:background">@color/white</item>
        <item name="android:titleTextStyle">@style/MyActionBarTitleText</item>
        <item name="android:logo">@drawable/custom_padding_logo_actionbar</item>
    </style>

    <style name="MyActionBarTitleText">
	    <item name="android:textColor">@color/white</item>
	 	<item name="android:textSize">22sp</item>
    </style>
    
    <!-- ActionBar tabs text styles -->
    <style name="MyActionBarTabText">
        <item name="android:textColor">@color/white</item>
    </style>
    
    <!-- Style Splash -->
	<style name="SplashTheem" parent="android:Theme">
	<item name="android:background">@drawable/custom_splash_center</item>
	<item name="android:windowNoTitle">true</item>
	</style>
</resources>
